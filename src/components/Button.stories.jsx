import { Button } from './Button';

const meta = { title: 'Components/Button', component: Button, args: { children: 'Continue' } };
export default meta;
export const Default = {};
export const Disabled = { args: { disabled: true } };
