# si669_inclass_3

A React Native component library with a browser-based Storybook.

## Scripts

- `npm run storybook` starts Storybook on port 6006.
- `npm run build` emits ESM, CommonJS, source maps, and declaration files to `dist`.
- `npm test` runs the Vitest suite.

Install the project dependencies before running these commands.
